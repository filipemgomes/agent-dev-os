# Architecture

OpenCode is the daily interface. Oh My OpenAgent owns orchestration, curated
agents, categories, model fallback, and delegation. Context Mode owns tool
output reduction and supported session snapshots. Context7 supplies current
library documentation. RuleSync owns only portable project rules, skills,
MCP declarations, hooks, subagents, and permissions. Git and project files are
the final source of truth.
