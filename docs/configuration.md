# Configuration

Portable preferences live in `.rulesync/`. Machine state lives under the
configured infrastructure root and is ignored by Git. OMO routing remains its
own upstream configuration and is deliberately not generated by RuleSync.
