# Security model

Existing configuration is backed up before modification. The installer does
not enable unrestricted sandbox access, never-approve policies, auto-push, or
auto-delete. Uninstall removes only the manifest's files and PATH entry; it
does not uninstall pre-existing coding agents.
